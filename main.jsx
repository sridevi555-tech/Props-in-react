import React from 'react'
import ReactDOM from 'react-dom/client'
import ParentComponent from './Components/ParentProp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ParentComponent/>
  </React.StrictMode>,
)
